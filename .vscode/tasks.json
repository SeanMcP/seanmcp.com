{
    "version": "2.0.0",
    "tasks": [
        {
            // VS Code will auto-detect and use pnpm
            "type": "npm",
            "script": "dev",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
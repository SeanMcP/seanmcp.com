---
import { readableDate } from "../utils";

const { date } = Astro.props;
---

<time datetime={date}>
  {readableDate(date)}
</time>

<style>
  time {
    color: var(--lighter-text-color);
  }
</style>

---
import Note from "../components/Note.astro";
import PageLayout from "./PageLayout.astro";

const {
  frontmatter: { date, edited, number },
} = Astro.props;

const title = `Note #` + number;
---

<PageLayout title={title}>
  <Note date={date} edited={edited} number={number}>
    <slot />
  </Note>
</PageLayout>
